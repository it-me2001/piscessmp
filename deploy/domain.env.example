# Copy to deploy/domain.env and fill in your values.
# Used by scripts/setup-caddy.sh for HTTPS + custom domain.

DOMAIN=play.example.com
ACME_EMAIL=admin@example.com

# Subdomains (optional — defaults shown)
VOICE_HOST=voice.example.com
MAP_HOST=map.example.com
